# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Local caches (downloaded on first run) — but scan results and history
# are committed by the scheduled GitHub Action so the hosted app gets them
data_cache/*
!data_cache/scan_results.csv
!data_cache/scan_info.csv
!data_cache/scan_meta.json
!data_cache/cpr_scan_results.csv
!data_cache/cpr_scan_info.csv
!data_cache/cpr_scan_meta.json
!data_cache/history/

# IDE / OS
.idea/
.vscode/
.DS_Store
*.swp

# Streamlit
.streamlit/secrets.toml
